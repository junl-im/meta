# v6.8.3 Cohesion & Integrity

- Stale rewrite draft guard when original/result changes after generation
- Rewrite draft/options session recovery across reload/update
- Collision-safe Fact Lock placeholders + model/code protection + 240-item safety cap
- Stronger local structural reflow while preserving facts and fixed hashtag/list blocks
- Tablet 980px bottom-sheet unification + desktop panel viewport clamping
- Review apply now closes panel and reveals result
- 20MB text import / 200k-character rewrite safety guards
- Escape-key panel dismissal and aria-live status improvements
- CI uses npm ci when a lockfile exists, otherwise npm install; browser-smoke failure diagnostics retained
- Delivery contract recorded: always provide FULL project + overwrite patch ZIP

- `⌨ 원본 그대로 쓰기`: original-only physical-keyboard verification; clipboard/drop/synthetic input blocked; only a 100% matching manually typed string can be applied to result
